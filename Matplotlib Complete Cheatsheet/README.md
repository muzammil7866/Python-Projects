# Matplotlib Complete Cheatsheet Guide

This folder contains a concept-focused notebook that serves as a practical Matplotlib reference.

## Notebook
- `Matplotlib_Complete_Cheatsheet_Guide.ipynb`

## Concepts Implemented
- Matplotlib introduction and setup
- MATLAB-like API (`pylab`) vs object-oriented API (`figure`, `axes`)
- Creating figures and axes (`add_axes`, `subplots`)
- Figure size, aspect ratio, and DPI
- Saving figures (`savefig`) in multiple formats (PNG, PDF, SVG, etc.)
- Titles, axis labels, and legends
- Color, line style, marker style, and linewidth customization
- Axis limits and plot range control
- Linear and logarithmic scales (`set_xscale`, `set_yscale`)
- Tick placement and custom tick labels
- Scientific notation formatting
- Axis spacing and layout adjustment (`subplots_adjust`)
- Grid control and styling
- Axis spines customization
- Twin axes (`twinx`, `twiny`)
- Axes crossing at zero
- Text annotations on plots
- Multiple subplots and layout systems
- `subplots`
- `subplot2grid`
- `gridspec`
- `add_axes` insets/manual placement
- 2D plot types and styles
- Line plots
- Scatter plots
- Bar plots
- Histograms
- Pie charts
- Colormap-based plotting
- `pcolor`
- `imshow`
- Contour plotting (`contour`)
- 3D plotting with `Axes3D`
- Surface plots
- Wireframe plots
- Contour plots with projections
- Camera/view angle control (`view_init`)
- Animation basics (`matplotlib.animation.FuncAnimation`)
- Matplotlib backends
- Non-interactive backends (file generation)
- Interactive backends (GUI)
- SVG backend usage
- Notebook inline backend (`%matplotlib inline`)
- Further reading/reference links

## Purpose
Use this notebook as a one-stop review guide for major Matplotlib concepts, from basic plotting to advanced layout, 3D visualization, and animations.


